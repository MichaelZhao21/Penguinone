<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.penguinone">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".QuizAfterActivity"></activity>
        <activity android:name=".DeodorantActivity" />
        <activity android:name=".DishSoapActivity" />
        <activity android:name=".KitchenActivity" />
        <activity android:name=".BedroomActivity" />
        <activity android:name=".HouseActivity" />
        <activity android:name=".QuizActivity" />
        <activity android:name=".TransitionActivity" />
        <activity android:name=".TitleActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>